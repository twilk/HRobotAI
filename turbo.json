{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "db:generate": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build", "db:generate"]
    },
    "test": {
      "dependsOn": ["^build", "db:generate"],
      "outputs": ["coverage/**"]
    },
    "test:integration": {
      "dependsOn": ["^build", "db:generate"],
      "cache": false,
      "env": ["CI", "POSTGRES_SUPERUSER_URL", "TENANT_DB_ENCRYPTION_KEY"]
    },
    "test:e2e:smoke": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "//#test:e2e:smoke": {
      "dependsOn": ["^build"],
      "cache": false,
      "env": ["CI", "E2E_BASE_URL", "E2E_USERNAME", "E2E_PASSWORD"]
    }
  }
}
