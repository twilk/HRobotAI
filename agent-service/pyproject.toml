[tool.pytest.ini_options]
# Put the service root on sys.path so `from app import ...` and `import tests` resolve whether
# pytest is invoked from /app in-container or from the repo checkout.
pythonpath = ["."]
testpaths = ["tests"]
