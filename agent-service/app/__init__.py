"""agent-service — self-learning scheduling agent (M2-C1 phase B skeleton).

Cold-start imitation (BC) + a working Gymnasium/RL loop layered on top of the FROZEN
ProblemInput/SolveResult contract and the live CP-SAT optimizer (``POST /solve``). This is the
first demonstrable increment, NOT a finished production RL brain — see ``README.md``.
"""
